# API base URL — swap module service mocks for live endpoints when ready
VITE_API_BASE_URL=https://api.auspre.local/v1
# Map tiles (OpenStreetMap by default; replace with Mapbox/your provider)
VITE_MAP_TILE_URL=https://{s}.tile.openstreetmap.org/{z}/{x}/{y}.png
